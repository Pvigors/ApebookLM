# 安全策略

## 支持范围

安全修复优先发布到默认分支和最新版本标签。旧版本不保证回补。

## 报告漏洞

请不要在公开 Issue、Discussion、聊天截图或日志中披露漏洞、密钥和用户数据。请使用 GitHub Private Vulnerability Reporting 提交私密报告，并包含：

- 受影响版本或提交
- 可复现步骤和最小请求
- 实际影响与所需权限
- 建议修复方向（如有）

维护者确认修复前，请避免公开利用细节。善意研究者在避免数据破坏、隐私侵犯和服务中断的前提下进行验证，项目将配合协调披露。

## 不应提交的内容

- API Key、Cookie、数据库连接串、私钥和访问令牌
- 用户上传资料、数据库、备份与生成媒体
- 真实生产域名、主机、账号和内部网络拓扑
- 未脱敏的日志、截图和评测报告

发现凭据泄露时，应先在供应商侧撤销或轮换；仅从最新提交删除并不能使历史中的凭据失效。
